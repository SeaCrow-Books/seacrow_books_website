<%= form_with(model: book_trope) do |form| %>
  <% if book_trope.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(book_trope.errors.count, "error") %> prohibited this book_trope from being saved:</h2>

      <ul>
        <% book_trope.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
