          <%= form_with model: @ai_model, local: true do |form| %>

            <div class="mb-3"> <!-- Updated class here -->
              <%= form.label :name, class: "form-label" %> <!-- Added class for label -->
              <%= form.text_field :name, class: "form-control" %>
            </div>

            <div class="mb-3"> <!-- Updated class here -->
              <%= form.label :description, class: "form-label" %> <!-- Added class for label -->
              <%= form.text_area :description, class: "form-control" %>
            </div>

            <%= form.submit "Save Changes", class: "btn btn-primary" %>
            <%= link_to 'Back to AI Models List', ai_models_path, class: 'btn btn-outline-secondary ms-2' %> <!-- Moved inside form and updated classes -->

          <% end %>