

    <div class="card">
      <div class="card-body">
          <%= form_for email_capture, html: { class: 'form__container' } do |f| %>

  <% if email_capture.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(email_capture.errors.count, "error") %> prohibited this email_capture from being saved:</h2>

      <ul>
        <% email_capture.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

            <div class="mt-3">
              <%= f.email_field :email, class: 'form-control form__input', placeholder: "Your best email...", required: true %>
            </div>
            <div class="mb-3 mt-2 d-grid gap-2">
              <%= f.submit 'Download free ebook', class: 'btn btn-primary' %>
            </div>
          <% end %>
      </div>
    </div>


