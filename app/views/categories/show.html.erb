<h1><%= @category.name %> Posts</h1>

<ul>
  <% @category.posts.each do |post| %>
    <li>
      <%= link_to post.title, post_path(post) %> <!-- Linking to the individual post's show page -->
    </li>
  <% end %>
</ul>

<%= link_to 'Back to Categories', categories_path %>
