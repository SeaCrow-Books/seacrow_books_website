<%= form_with(model: @book_genre, local: true) do |form| %>
  <% if @book_genre.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@book_genre.errors.count, "error") %> prohibited this book genre from being saved:</h2>

      <ul>
      <% @book_genre.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
